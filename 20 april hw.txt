1)
import java.util.Scanner;
class Sum
{
 public static void main(String [] args)
 {
   System.out.println("enter first number:");
   Scanner sc=new Scanner(System.in);
   int One=sc.nextInt();
   System.out.println("enter second number:");
   int Second=sc.nextInt();
   int sum = One+Second;
   System.out.println("sum of two numbers are :" + sum);
 }
}
2)
import java.util.Scanner;
class StoreArray
{
 public static void main(String [] args)
 {
   Scanner sc=new Scanner(System.in);
   //creating arrray
   int numbers[] = new int[3];
   System.out.println("please enter 3 integer values(press enter after each value)");
   //stroing inputs in array
   for (int i=0;i<numbers.length;i++)
   {
	   numbers[i]=sc.nextInt();
   }
   System.out.println("numbers are:");
   //print array
   for (int i=0;i<numbers.length;i++)
   {
	   System.out.println(numbers[i] + "");
   }
 } 
}
3)
import java.util.Scanner;
class MenuDrivenArray
{
 public static void main(String [] args)
 {
    Scanner sc = new Scanner(System.in);
        int[] arr = new int[50]; 
        int size = 0;
        int choice;

        do {
            System.out.println("\n--- MENU ---");
            System.out.println("1. Insert elements");
            System.out.println("2. Display elements");
            System.out.println("3. Count Even numbers");
            System.out.println("4. Count Odd numbers");
            System.out.println("5. Exit");
            System.out.print("Enter your choice: ");
            choice = sc.nextInt();

            switch (choice) {
                case 1:
                    System.out.print("How many elements to insert? ");
                    int n = sc.nextInt();
                    for (int i = 0; i < n; i++) {
                        System.out.print("Enter element " + (size + 1) + ": ");
                        arr[size++] = sc.nextInt();
                    }
                    break;

                case 2:
                    System.out.print("Array elements: ");
                    for (int i = 0; i < size; i++) {
                        System.out.print(arr[i] + " ");
                    }
                    System.out.println();
                    break;

                case 3:
                    int evenCount = 0;
                    for (int i = 0; i < size; i++) {
                        if (arr[i] % 2 == 0) evenCount++;
                    }
                    System.out.println("Total Even numbers: " + evenCount);
                    break;

                case 4:
                    int oddCount = 0;
                    for (int i = 0; i < size; i++) {
                        if (arr[i] % 2 != 0) oddCount++;
                    }
                    System.out.println("Total Odd numbers: " + oddCount);
                    break;

                case 5:
                    System.out.println("Exiting program...");
                    break;

                default:
                    System.out.println("Invalid choice! Try again.");
            }
        } while (choice != 5);
 } 
}